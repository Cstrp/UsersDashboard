import { Card } from '@mui/material';
import { SignInForm } from '../../components';

export const SignIn = () => {
  return (
    <Card className={'p-5 w-1/2 h-2/6'}>
      <SignInForm />
    </Card>
  );
};
