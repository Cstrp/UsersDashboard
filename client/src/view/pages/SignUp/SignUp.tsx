import { SignUpForm } from '../../components';
import { Card } from '@mui/material';

export const SignUp = () => {
  return (
    <Card className={'p-5 w-1/2 h-1/2'}>
      <SignUpForm />
    </Card>
  );
};
