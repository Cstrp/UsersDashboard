import { SignInForm } from '../../components';
import { Card } from '@mui/material';

export const Sign_In = () => {
  return (
    <Card className={'p-5 w-1/2 h-2/5'}>
      <SignInForm />
    </Card>
  );
};
